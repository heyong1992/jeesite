<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.student.dao.TStudentDao">
    
	<sql id="tStudentColumns">
		a.id AS "id",
		a.name AS "name",
		a.age AS "age",
		a.cid AS "cid"
	</sql>
	
	<sql id="tStudentJoins">
	</sql>
    
	<select id="get" resultType="TStudent">
		SELECT 
			<include refid="tStudentColumns"/>
		FROM t_student a
		<include refid="tStudentJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TStudent">
		SELECT 
			<include refid="tStudentColumns"/>
		FROM t_student a
		<include refid="tStudentJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="TStudent">
		SELECT 
			<include refid="tStudentColumns"/>
		FROM t_student a
		<include refid="tStudentJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO t_student(
			id,
			name,
			age,
			cid
		) VALUES (
			#{id},
			#{name},
			#{age},
			#{cid}
		)
	</insert>
	
	<update id="update">
		UPDATE t_student SET 	
			name = #{name},
			age = #{age},
			cid = #{cid}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM t_student
		WHERE id = #{id}
	</update>
	
</mapper>