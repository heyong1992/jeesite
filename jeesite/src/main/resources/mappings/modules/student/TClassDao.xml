<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.student.dao.TClassDao">
    
	<sql id="tClassColumns">
		a.id AS "id",
		a.name AS "name"
	</sql>
	
	<sql id="tClassJoins">
	</sql>
    
	<select id="get" resultType="TClass">
		SELECT 
			<include refid="tClassColumns"/>
		FROM t_class a
		<include refid="tClassJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TClass">
		SELECT 
			<include refid="tClassColumns"/>
		FROM t_class a
		<include refid="tClassJoins"/>
		<where>
			
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{name},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="TClass">
		SELECT 
			<include refid="tClassColumns"/>
		FROM t_class a
		<include refid="tClassJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO t_class(
			id,
			name
		) VALUES (
			#{id},
			#{name}
		)
	</insert>
	
	<update id="update">
		UPDATE t_class SET 	
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM t_class
		WHERE id = #{id}
	</update>
	
</mapper>